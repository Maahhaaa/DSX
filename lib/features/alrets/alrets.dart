import 'package:flutter/material.dart';

class Alrets extends StatelessWidget {
  const Alrets({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text("Alrets"),
    );
  }
}